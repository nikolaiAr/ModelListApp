<UserControl x:Class="ModelListApp.View.ParcelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelListApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="5">
            <Label Content="Number" Margin="1"/>
            <TextBox Text="{Binding Parcel.Number}" Margin="1"/>
            <Label Content="Location" Margin="1"/>
            <TextBox Text="{Binding Parcel.Location}" Margin="1"/>
        </StackPanel>
    </Grid>
</UserControl>
